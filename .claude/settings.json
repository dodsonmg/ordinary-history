{
  "permissions": {
    "allow": [
      "Bash(gh run watch:*)",
      "Bash(gh run list:*)"
    ]
  },
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "if": "Bash(git push*)",
            "command": "echo '{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"additionalContext\":\"Reminder: before this push completes, confirm the changes are consistent with CLAUDE.md, site-plan.md Status lines, and README.md Status section — update any that have gone stale as part of this push, not as follow-up work.\"}}'"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "if": "Bash(git push*)",
            "command": "bash .claude/hooks/post-push-verify-deploy.sh",
            "timeout": 300,
            "statusMessage": "Watching GitHub Actions deploy..."
          }
        ]
      }
    ]
  }
}
